// Strings

let firstName = "Daniel"
let lastName = "Moreno"

// .concat(object to concat)
let fullName = firstname.concat(lastName)

console.log(fullName)

alphabet = "abcdefghijklmnopqrstuvwxyz"
// .split(character to split at)
let char = alphabet.split("")

// .length counts how many characters in a string
alphabet.length // will output 26

// Numbers

let value = 100

// Arrays

let collectionOfPeople = ["job","jane","dave"]
collectionOfPeople[0] // pulls first index position
